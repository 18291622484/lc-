int main() {
    /*int maxDepth(struct TreeNode* root) {
        if (root == NULL) {
            return 0;
        }
        int _left = maxDepth(root->left);
        int _right = maxDepth(root->right);
        int ret = _left > _right ? _left + 1 : _right + 1;
        return ret;
    }*/
	return 0;
}