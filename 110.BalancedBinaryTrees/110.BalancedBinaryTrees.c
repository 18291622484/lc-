int main() {
    /*int deep(struct TreeNode* root) {
        if (root == NULL) {
            return 0;
        }
        int _left = deep(root->left);
        int _right = deep(root->right);
        return _left > _right ? _left + 1 : _right + 1;
    }

    bool isBalanced(struct TreeNode* root) {
        if (root == NULL) {
            return true;
        }
        int __left = deep(root->left);
        int __right = deep(root->right);
        return abs(__left - __right) < 2
            && isBalanced(root->left)
            && isBalanced(root->right);
    }*/
	return 0;
}