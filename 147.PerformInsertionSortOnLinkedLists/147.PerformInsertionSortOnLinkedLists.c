int main() {
    //struct ListNode* insertionSortList(struct ListNode* head) {
    //    if (head == NULL || head->next == NULL) {
    //        return head;
    //    }
    //    struct ListNode list;
    //    list.next = head;
    //    struct ListNode* tail = head;
    //    struct ListNode* cur = tail->next;
    //    while (cur) {
    //        if (cur->val >= tail->val) {
    //            tail = tail->next;
    //            cur = cur->next;
    //        }
    //        else {
    //            struct ListNode* node = cur;
    //            cur = cur->next;
    //            tail->next = cur;
    //            struct ListNode* swap = list.next;
    //            struct ListNode* last = &list;
    //            while (swap != cur) {
    //                if (swap->val >= node->val) {
    //                    last->next = node;
    //                    node->next = swap;
    //                    break;
    //                }
    //                last = swap;
    //                swap = swap->next;
    //            }
    //        }
    //    }
    //    return list.next;
    //}
	return 0;
}