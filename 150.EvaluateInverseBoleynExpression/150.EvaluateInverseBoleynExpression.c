int main() {
    //bool func(char* c) {
    //    if ((*c == '+' || *c == '-' || *c == '*' || *c == '/') && *(c + 1) == '\0') {
    //        return false;
    //    }
    //    return true;
    //}
    //int returnNum(int a, int b, char c) {
    //    if (c == '+') {
    //        return a + b;
    //    }
    //    else if (c == '-') {
    //        return a - b;
    //    }
    //    else if (c == '*') {
    //        return a * b;
    //    }
    //    return a / b;
    //}
    //int evalRPN(char** tokens, int tokensSize) {
    //    int* stack = (int*)malloc(sizeof(int) * tokensSize);
    //    for (int i = 0; i < tokensSize; i++) {
    //        if (func(tokens[i])) {
    //            *stack++ = atoi(tokens[i]);
    //        }
    //        else {
    //            int b = *(--stack);
    //            int a = *(--stack);
    //            *stack++ = returnNum(a, b, *tokens[i]);
    //        }
    //    }
    //    return *(--stack);
    //}
	return 0;
}