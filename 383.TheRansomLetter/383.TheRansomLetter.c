int main() {
    /*bool canConstruct(char* ransomNote, char* magazine) {
        int ranarr[26] = { 0 };
        int magarr[26] = { 0 };
        while (*ransomNote) {
            int i = (int)(*ransomNote);
            ranarr[i - 97]++;
            ransomNote++;
        }
        while (*magazine) {
            int i = (int)(*magazine);
            magarr[i - 97]++;
            magazine++;
        }
        for (int i = 0; i < 26; i++) {
            if (ranarr[i] && ranarr[i] > magarr[i]) {
                return false;
            }
        }
        return true;
    }*/
	return 0;
}