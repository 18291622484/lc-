int main() {
    //int myAtoi(char* s) {
    //    long long num = 0;
    //    char ch = '+';
    //    int flag = 0;
    //    while (*s && flag < 2 && num < 0x90000000) {
    //        if (flag < 1 && *s == ' ') {
    //            s++;
    //        }
    //        else if (flag < 1 && (*s == '-' || *s == '+')) {
    //            ch = *s++;
    //            flag++;
    //        }
    //        else if (*s >= '0' && *s <= '9') {
    //            num *= 10;
    //            num += *s++ - '0';
    //            flag == 1 ? flag : flag++;
    //        }
    //        else {
    //            break;
    //        }
    //    }
    //    if (ch == '+' && num >= 0x7fffffff) {
    //        return 0x7fffffff;
    //    }
    //    if (ch == '-' && num >= 0x80000000) {
    //        return -0x80000000;
    //    }
    //    return ch == '-' ? -(int)num : (int)num;
    //}
	return 0;
}