int main() {
    /*bool isLongPressedName(char* name, char* typed) {
        char* n = name;
        char* t = typed;
        while (*n || *t) {
            if (*t == *n || (n > name && (*t == *(n - 1) || *(n - 1) == *t))) {
                if (*t == *n) {
                    n++;
                }
                t++;
            }
            else {
                return false;
            }
        }
        return true;
    }*/
	return 0;
}