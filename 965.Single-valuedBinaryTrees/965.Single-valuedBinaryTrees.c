int main() {
    /*bool _isSame(struct TreeNode* root, int n) {
        if (root) {
            return root->val == n
                && _isSame(root->left, n)
                && _isSame(root->right, n);
        }
        return true;
    }

    bool isUnivalTree(struct TreeNode* root) {
        if (root == NULL) {
            return true;
        }
        int n = root->val;
        return _isSame(root, n);
    }*/

    return 0;
}