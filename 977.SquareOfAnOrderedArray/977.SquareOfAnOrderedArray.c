int main() {
    /*int* sortedSquares(int* nums, int numsSize, int* returnSize) {
        int* ret = (int*)malloc(sizeof(int) * numsSize);
        int m = 0;
        int n = numsSize - 1;
        int count = n;
        while (m <= n) {
            if (nums[m] + nums[n] >= 0) {
                ret[count--] = nums[n] * nums[n];
                n--;
            }
            else {
                ret[count--] = nums[m] * nums[m];
                m++;
            }
        }
        *returnSize = numsSize;
        return ret;
    }*/
	return 0;
}